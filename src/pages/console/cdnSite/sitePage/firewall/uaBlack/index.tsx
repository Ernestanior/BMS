import React, { FC, ReactElement } from "react";
import "./index.less";

const Index: FC = (): ReactElement => {
  return <div style={{ height: "200px" }}>UA blacklist</div>;
};

export default Index;
